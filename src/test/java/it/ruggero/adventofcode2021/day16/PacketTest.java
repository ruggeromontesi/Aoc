package it.ruggero.adventofcode2021.day16;

import org.junit.jupiter.api.Test;

public class PacketTest {

    private static final String PACKET_1_HEX = "D2FE282";

    @Test
    void shouldBuildPacket() {

        //PacketFactory.builder().inputString_hex(PACKET_1_HEX).build().;
         //run(PACKET_1_HEX);
         //var packets = getPackets();
          //assertThat(packets).hasSize(3);


//        var packet = new Packet(PACKET_1_HEX).buildPacket();
//        int version = packet .getVersion();
//        assertEquals(version, 6);
//        assertEquals(packet.getTypeId(), 4);
//        assertThat(packet.getBitGroupList()).hasSize(3);
//        assertThat(packet.getLiteralValue()).isEqualTo(2021);



    }
}
